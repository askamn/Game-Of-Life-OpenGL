#include <Windows.h>
#include <vector>
#include <iostream>
int CALLBACK WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdArgs, int nCmdShow)
{
	WNDCLASSEX WindowClass;

	WindowClass.cbSize = sizeof(WNDCLASSEX);
	//CreateWindowEx;
}